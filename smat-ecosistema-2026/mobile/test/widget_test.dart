// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility in the flutter_test package. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

//import 'package:flutter/material.dart';
//import 'package:flutter_test/flutter_test.dart';

//import 'package:mobile/main.dart'; // Asegúrate de que tu proyecto se llame 'mobile'

//void main() {
  //testWidgets('Prueba inicial de SMATApp', (WidgetTester tester) async {
    // Aquí cambiamos MyApp() por SMATApp()
    //await tester.pumpWidget(const SMATApp());

    // Verifica que la AppBar existe
    //expect(find.text('SMAT - Monitoreo Móvil'), findsOneWidget);
  //});
//}
